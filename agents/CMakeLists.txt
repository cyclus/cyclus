
INCLUDE_DIRECTORIES(${CYCLUS_CORE_INCLUDE_DIRS} ${CYCLUS_CORE_TEST_INCLUDE_DIRS})
use_cyclus("NullInst" "null_inst")
use_cyclus("NullRegion" "null_region")
use_cyclus("Sink" "sink")
use_cyclus("Source" "source")
